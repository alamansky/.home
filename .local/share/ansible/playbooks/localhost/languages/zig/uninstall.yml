tasks:
  - name: uninstall zig
    hosts: localhost
    vars_files:
      - data.yml
    tasks:
      - name: uninstall language
        include_tasks:
          file: ../../library/tasks/uninstall_language.yml
